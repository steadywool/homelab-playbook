---
- hosts: games
  gather_facts: true
  become: true

  tasks:
    - name: Execute common tasks
      ansible.builtin.import_tasks: tasks/common.yml
      tags: common

    - name: Deploy Minecraft server
      ansible.builtin.import_tasks: tasks/minecraft.yml
      tags: minecraft