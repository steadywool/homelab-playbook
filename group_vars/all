---
# Containers directories
container_root: "{{ lookup('env','HOME') }}/container_root"

container_volumes:
    caddy: "{{ container_root }}/caddy"
    syncthing: "{{ container_root }}/syncthing"
    wireguard: "{{ container_root }}/wireguard"
    nextcloud: "{{ container_root }}/nextcloud"
    minecraft: "{{ container_root }}/minecraft"