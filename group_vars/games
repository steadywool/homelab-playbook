---
packages:
    - docker-ce
    - docker-ce-cli
    - containerd.io
    - python3-docker
    - qemu-guest-agent

services:
    - docker.service
    - qemu-guest-agent.service

volumes:
    minecraft: "/home/{{ remote_user }}/containers/minecraft"