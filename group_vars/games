---
packages:
    - podman
    - qemu-guest-agent

services:
    system:
        - qemu-guest-agent.service
    user:
        - podman-restart.service

volumes:
    minecraft: "/home/{{ ansible_user }}/container_root/minecraft"