---
packages:
    - docker.io
    - qemu-guest-agent

services:
    - docker.service
    - qemu-guest-agent.service

volumes:
    caddy: "/home/{{ remote_user }}/containers/caddy"
    syncthing: "/home/{{ remote_user }}/containers/syncthing"
    filebrowser: "/home/{{ remote_user }}/containers/filebrowser"

# Firefly III token
fireflyiii_token: CHANGEME32CHAR