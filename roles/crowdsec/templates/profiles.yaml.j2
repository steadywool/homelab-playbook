name: default_ip_remediation
#debug: true
filters:
 - Alert.Remediation == true && Alert.GetScope() == "Ip"
decisions:
 - type: ban
   duration: 4h
notifications:
  - gotify  # Set the webhook in /etc/crowdsec/notifications/gotify.yaml before enabling this.
on_success: break
---
name: default_range_remediation
#debug: true
filters:
 - Alert.Remediation == true && Alert.GetScope() == "Range"
decisions:
 - type: ban
   duration: 4h
notifications:
  - gotify  # Set the webhook in /etc/crowdsec/notifications/gotify.yaml before enabling this.
on_success: break