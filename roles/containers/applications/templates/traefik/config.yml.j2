---
http:
  routers:
    proxmox:
      rule: "Host(`pve.{{ private_domain }}`)"
      entryPoints: 
        - "https"
      tls: {}
      service: "proxmox"

  services:
    proxmox:
      loadBalancer:
        servers:
          - url: "https://{{ groups['pve'][0] }}:8006"