---
ungrouped:
  hosts:
    srv-truenas:
      # Connection informations
      ansible_host: "{{ vault_inventory['srv-truenas']['host'] }}"
      ansible_user: "{{ vault_inventory['srv-truenas']['user'] }}"

docker:
  hosts:
    vm-docker:
      # Connection informations
      ansible_host: "{{ vault_inventory['vm-docker']['host'] }}"
      ansible_user: "{{ vault_inventory['vm-docker']['user'] }}"
      ansible_become_password: "{{ vault_inventory['vm-docker']['password'] }}"

      # Common variables
      additional_packages:
        - nfs-common
        - qemu-guest-agent
      additional_services:
        - qemu-guest-agent.service

    vm-games:
      # Connection informations
      ansible_host: "{{ vault_inventory['vm-games']['host'] }}"
      ansible_user: "{{ vault_inventory['vm-games']['user'] }}"
      ansible_become_password: "{{ vault_inventory['vm-games']['password'] }}"

      # Common variables
      additional_packages:
        - qemu-guest-agent
      additional_services:
        - qemu-guest-agent.service