[ungrouped]
pbs ansible_host="{{ inventory.pbs.host }}" ansible_user="{{ inventory.pbs.user }}"
pve ansible_host="{{ inventory.pve.host }}" ansible_user="{{ inventory.pve.user }}"
nas ansible_host="{{ inventory.nas.host }}" ansible_user="{{ inventory.nas.user }}"
vpn ansible_host="{{ inventory.vpn.host }}" ansible_user="{{ inventory.vpn.user }}"
apps ansible_host="{{ inventory.apps.host }}" ansible_user="{{ inventory.apps.user }}" ansible_become_password="{{ inventory.apps.pass }}"
games ansible_host="{{ inventory.games.host }}" ansible_user="{{ inventory.games.user }}" ansible_become_password="{{ inventory.games.pass }}"

[docker]
vpn
apps
games